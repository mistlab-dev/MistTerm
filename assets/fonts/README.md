# CJK 字体（内置回退）

| 文件 | 用途 |
|------|------|
| `NotoSansSC-Regular.otf` | UI / 图标（`include_bytes!`，约 8MB） |
| `NotoSansSC-Regular.ttf` | PDF 导出（`genpdf` 需 TTF，Fontsource 子集约 2MB，OFL-1.1） |

重新下载：

```bash
./scripts/fetch-cjk-font.sh
```
